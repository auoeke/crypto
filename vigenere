#!/bin/bash
j=0

for ((i = 0; i < ${#1}; i++))
do
    
    ciphertext+=$(./shiftchar "${1:i:1}" "${2:j:1}")
    let "j++"

    if [[ $j == ${#2} ]]
    then
        j=0
    fi
done

echo "${ciphertext}"
